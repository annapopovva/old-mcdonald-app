export class Animal {
   public type: string;
   public sound: string;
   public image: string;

   constructor(type: string, sound: string, image: string) {
       this.type = type;
       this.sound = sound;
       this.image = image;
   }
}